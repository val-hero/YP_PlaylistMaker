<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.PlaylistMaker" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/white</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="MainScreen" parent="Theme.PlaylistMaker">
        <item name="android:background">@color/yp_blue</item>
    </style>

    <style name="SettingsScreen" parent="Theme.PlaylistMaker">
        <item name="drawableTint">@color/yp_gray</item>
        <item name="android:textColor">?attr/colorOnPrimary</item>
        <item name="thumbTint">@color/yp_gray</item>
        <item name="trackTint">@color/yp_light_gray</item>
    </style>

</resources>